//! Facebook phone interview question
//! Given a N * N matrix where each row is sorted. columns are not sorted
//! Find the median
#include "../../util/common.h"

class Solution {
public:
    double findMedian(vector<vector<int>> &matrix) {

    }
};

int main() {
    return 0;
}